# ================================================================================
# Namespace - Isolation pour l'application CodeArena
# ================================================================================
# Crée un namespace dédié pour isoler toutes les ressources de CodeArena
# ================================================================================

apiVersion: v1
kind: Namespace
metadata:
  name: codearena
  labels:
    name: codearena
    environment: production
    app: codearena
